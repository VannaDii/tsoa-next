---
'@tsoa-next/cli': patch
'@tsoa-next/runtime': patch
'tsoa-next': patch
---

Trigger a development publish from the main-branch dev build workflow so downstream projects can validate prerelease packages before the next public release.
